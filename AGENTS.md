# AGENTS.md — правила для AI-агентов

Правила для AI-агентов (Codex, OpenCode, Roo, Kilo, Pi и др.) по работе с данным пакетом.

---

## Проект

**prikotov/git-workflow** — PHP-пакет: конвенции и правила работы с Git (ветки, коммиты, PR, ревью, релизы, деплой).

Пакет подключается к проекту-потребителю через `composer require --dev` и init-скрипт `bin/git-workflow-init`.

### Состав

1. **Init-скрипт** (`bin/git-workflow-init`) — CLI-команда, копирует документацию в проект-потребитель.
2. **Документация** (`docs/git-workflow/`) — конвенции по Git workflow, копируемые init-скриптом.

---

## Перед каждым пушем / PR

- **Проверять `composer.json`** на приватные зависимости — в CI нет доступа к VCS-репозиториям.
- **Запускать CI** — `.github/workflows/ci.yml`.

---

## Принципы работы с кодом

- **PHP 8.4**, строгая типизация (`declare(strict_types=1)`).
- **Читаемость** важнее производительности. Осмысленные имена, минимальная вложенность.
- **Стабильность**: init-скрипт должен быть обратно совместим — проекты-потребители не должны ломаться при обновлении.

---

## Структура пакета

```
bin/
  git-workflow-init          # CLI-команда инициализации
docs/
  git-workflow/              # Документация, копируемая в проект-потребитель
    index.md                 # Оглавление раздела
    branches.md              # Ветки: типы, именование, жизненный цикл
    commits.md               # Conventional Commits: формат и правила
    pull-request.md          # Процесс PR и требования
    code-review.md           # Чеклист и правила ревью
    release.md               # SemVer, модель релизов, CHANGELOG
    deploy.md                # Правила деплоя
    release-checklists.md    # Чеклисты для релиза, hotfix, деплоя
    releases/                # Артефакты релиза
      index.md
      templates/release-plan.template.md
    templates/
      release-plan.template.md
todo/                        # Внутренние задачи по доработке пакета
```

---

## Правила написания документации

### Язык и стиль

- **Русский** с английским термином в скобках при первом упоминании.
- Формулировки как «пули» — короткие, чёткие, без воды.
- Каждый документ описывает **один процесс** или **одну сущность**.

### Форматирование

- **Markdown** для всех документов.
- Ссылки между документами — относительные (от текущего файла).
- Примеры команд — в кодовых блоках с указанием языка.

---

## Init-скрипт (`bin/git-workflow-init`)

### Ключевые принципы

- **Idempotent** — повторный запуск безопасен, существующие файлы не перезаписываются (кроме `--force`).
- **Минимальные зависимости** — только PHP 8.4, без внешних библиотек.
- **Аргументы**: `[target-dir]`, `--docs-path=<path>`, `--force`.

### Что делает

1. Копирует `docs/git-workflow/` в `docs/git-workflow/` проекта-потребителя.
2. Создаёт `docs/releases/` с `.gitkeep` для артефактов релиза.
3. Обновляет `.gitignore` в `docs/`.

---

## Задачи по доработке пакета

Внутренние задачи хранятся в `todo/` в формате todo-md (TASK-*.todo.md).

---

## Ссылки

- **README**: [README.md](README.md)
- **Документация пакета**: [docs/git-workflow/index.md](docs/git-workflow/index.md)
- **Init-скрипт**: [bin/git-workflow-init](bin/git-workflow-init)
