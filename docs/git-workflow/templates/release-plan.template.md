# План релиза vX.Y.Z

## Метаданные

- Тег релиза: `vX.Y.Z`
- Тип повышения версии (указать одно: `major`, `minor` или `patch`):
- Обоснование выбранной версии:
- Линия релиза: `release/x.y`
- Рабочая ветка подготовки: `task/…` от `release/x.y`
- PR подготовки релиза в `release/x.y`:
- Ответственный:
- Плановая дата deploy:

## Состав

- Включённые PR:
- Включённые задачи:
- Вне состава релиза:

## Риски

- Основные риски:
- Наличие миграций данных:
- Порядок применения миграций:
- Риск окна несовместимости:
- Замечания по обратной совместимости:

## Порядок deploy

1. Web
2. Workers

## Проверки перед deploy

- Этот план и остальные релизные файлы включены через PR до создания тега
- Окончательное состояние PR одобрено; обязательные проверки и CI слитого коммита `release/x.y` успешны
- Тег релиза создан на этом проверенном коммите; в `origin` опубликован только конкретный тег
- Подготовлены обязательные изменения в env
- В документе зафиксированы миграции, порядок их применения и риск окна несовместимости
- Подготовлены команды health-check

## Проверки после deploy

- Основные пользовательские сценарии:
- Логи:
- Очереди и воркеры:
- Build version и git SHA:

## Действия при проблеме после релиза

- Откат: не используется
- Стратегия исправления: `hotfix` / `patch release`
- Ответственный инженер:
- Канал коммуникации / задача:

## Заметки

- Дополнительные инструкции:
